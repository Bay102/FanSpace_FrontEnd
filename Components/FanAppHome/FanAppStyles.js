import { StyleSheet } from "react-native";

export const FanAppStyles = StyleSheet.create({

   container: {
      flex: 1,
      justifyContent: "center",
      alignItems: 'center',
      width: '100%',
      height: '100%',
      borderWidth: 2,
   
   }
})